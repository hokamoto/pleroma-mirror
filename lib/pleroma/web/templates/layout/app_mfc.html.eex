<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1,minimal-ui" />
  <title>
  <%= Application.get_env(:pleroma, :instance)[:name] %>
  </title>
  <style>
    body {
      background-color: #404550;
      font-family: sans-serif;
      color: #205030;;
      text-align: center;
    }

    .container {
      max-width: 420px;
      padding: 20px;
      background-color: #f0f0f4;
      border-radius: 4px;
      margin: auto;
      margin-top: 10vh;
      box-shadow: 0 1px 3px 0px rgba(0, 0, 0, 0.3), 0 2px 8px 0px rgba(0, 0, 0, 0.4);
    }

    h1 {
      margin: 0;
      font-size: 24px;
    }

    h2 {
      color: #305040;
      font-weight: normal;
      font-size: 18px;
      margin-bottom: 20px;
    }

    form {
      width: 100%;
    }

    .input {
      text-align: left;
      color: #687868;
      display: flex;
      flex-direction: column;
    }

    input {
      box-sizing: content-box;
      padding: 10px;
      margin-top: 5px;
      margin-bottom: 10px;
      background-color: #e0e0e8;
      color: #305040;
      border: 0;
      transition-property: border-bottom;
      transition-duration: 0.35s;
      border-bottom: 2px solid #b0b0c0;
      font-size: 14px;
    }

    .scopes-input {
      display: flex;
      flex-direction: column;
      margin-top: 1em;
      text-align: left;
      color: #687868;
    }

    .scopes-input label:first-child {
      height: 2em;
    }

    .scopes {
      display: flex;
      flex-wrap: wrap;
      text-align: left;
      color: #305040;
    }

    .scope {
      display: flex;
      flex-basis: 100%;
      height: 2em;
      align-items: center;
    }

    .scope:before {
      color: #98b898;
      content: "✔";
      margin-left: 1em;
      margin-right: 1em;
    }

    [type="checkbox"] + label {
      display: none;
      cursor: pointer;
      margin: 0.5em;
    }

    [type="checkbox"] {
      display: none;
    }

    [type="checkbox"] + label:before {
      cursor: pointer;
      display: inline-block;
      color: white;
      background-color: #e0e0e8;
      border: 4px solid #e0e0e8;
      box-shadow: 0px 0px 1px 0 #006900;
      box-sizing: border-box;
      width: 1.2em;
      height: 1.2em;
      margin-right: 1.0em;
      content: "";
      transition-property: background-color;
      transition-duration: 0.35s;
      color: #121a24;
      margin-bottom: -0.2em;
      border-radius: 2px;
    }

    [type="checkbox"]:checked + label:before {
      background-color: #006900;
    }

    input:focus {
      outline: none;
      border-bottom: 2px solid #006900;
    }

    button {
      box-sizing: border-box;
      width: 100%;
      background-color: #086f08;
      color: #f0fff0;
      border-radius: 4px;
      border: none;
      padding: 10px;
      margin-top: 20px;
      margin-bottom: 20px;
      text-transform: uppercase;
      font-size: 16px;
      box-shadow: 0px 1px 2px 0px #505050,
        0px 2px 0px 0px rgba(255, 255, 255, 0.25) inset,
        0px -2px 0px 0px rgba(0, 0, 0, 0.2) inset,
        0px 12px 20px 0px rgba(128, 255, 128, 0.2) inset;
    }

    button:hover {
      cursor: pointer;
      background-color: #148014;
    }

    .alert-danger {
      box-sizing: border-box;
      width: 100%;
      background-color: #e3c0c4;
      border: 1px solid #a06060;
      color: #902020;
      border-radius: 4px;
      padding: 10px;
      margin-top: 20px;
      font-weight: 500;
      font-size: 16px;
    }

    .alert-info {
      box-sizing: border-box;
      width: 100%;
      border-radius: 4px;
      border: 1px solid #a0d0a0;
      padding: 10px;
      margin-top: 20px;
      font-weight: 500;
      font-size: 16px;
    }

    @media all and (max-width: 440px) {
      .container {
        margin-top: 0
      }

      .scope {
        flex-basis: 0%;
      }

      .scope:before {
        content: "";
        margin-left: 0em;
        margin-right: 1em;
      }

      .scope:first-child:before {
        margin-left: 1em;
        content: "✔";
      }

      .scope:after {
        content: ",";
      }

      .scope:last-child:after {
        content: "";
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>MFC Social</h1>
    <%= render @view_module, @view_template, assigns %>
  </div>
</body>
</html>
